services:
  email-analytics:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - email-analytics-cache:/app/cache
    env_file:
      - path: .env.local
        required: false
    restart: unless-stopped

volumes:
  email-analytics-cache:
