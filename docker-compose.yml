services:
  email-analytics:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - email-analytics-cache:/app/cache
    restart: unless-stopped

volumes:
  email-analytics-cache:
